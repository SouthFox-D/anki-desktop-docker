sudo chown -R abc:abc /config/app &&
 mkdir -p /config/app/Anki2 &&
 mkdir -p /config/app/Anki &&
 mkdir -p /config/app/syncserver &&
 export DISABLE_QT5_COMPAT=1 &&
export LC_ALL=en_US.UTF-8 && anki --syncserver
